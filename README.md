# Chandramuke-task6
Implement K-Nearest Neighbors (KNN) for classification using Scikit-learn. Select and normalize a dataset, apply KNeighborsClassifier, and test various K values. Evaluate performance with accuracy and a confusion matrix. Visualize decision boundaries to understand model behavior and classification zones.
